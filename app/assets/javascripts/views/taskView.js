var TaskView = function(task){
  this.task = task;
  this.$el = $("<div class='task'></div>")
};

ListView.prototype.render = function(){
  var self = this;
  //self.$el.html.taskTemplate(self.task);
}
